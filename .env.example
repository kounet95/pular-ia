# ─────────────────────────────────────────────────────────────────────────────
# PULAR IA — Configuration
# 1. Copie ce fichier : cp .env.example .env
# 2. Remplis tes identifiants
# 3. Ne commite JAMAIS le fichier .env sur Git !
# ─────────────────────────────────────────────────────────────────────────────

# ── Web App ───────────────────────────────────────────────────────────────────
WEBAPP_PORT=8080

# Modèle Whisper : tiny (rapide, moins précis) | base | small | medium
WHISPER_MODEL_BOT=tiny

# ── Telegram — Scraper (optionnel) ───────────────────────────────────────────
# Obtenir sur https://my.telegram.org → "API development tools"
TELEGRAM_API_ID=
TELEGRAM_API_HASH=
TELEGRAM_PHONE=

# ── Telegram — Bot communautaire (optionnel) ──────────────────────────────────
# Obtenir via @BotFather → /newbot
TELEGRAM_BOT_TOKEN=

# ── Production (Railway / Render / VPS) ──────────────────────────────────────
# URL publique de l'app (sans slash final)
# PUBLIC_URL=https://pular-ia.railway.app

# Clé admin pour les endpoints /api/admin/* (export corpus, génération dataset)
# Génère une clé aléatoire : python -c "import secrets; print(secrets.token_hex(32))"
# Laisser vide = pas de protection (OK pour test local, DANGEREUX en prod)
ADMIN_KEY=

# ── Espace Éditorial — Paiement Stripe (optionnel) ───────────────────────────
# Clé secrète Stripe (dashboard.stripe.com → Développeurs → Clés API)
# Peut être partagée avec un autre projet utilisant le même compte Stripe.
STRIPE_SECRET_KEY=
# Secret du endpoint webhook DÉDIÉ à cette app (dashboard.stripe.com →
# Développeurs → Webhooks → Ajouter un endpoint :
# <PUBLIC_URL>/api/editorial/webhook → événement "checkout.session.completed").
# Nom volontairement distinct de STRIPE_WEBHOOK_SECRET pour ne pas entrer en
# conflit si le compte Stripe est aussi utilisé par un autre projet.
STRIPE_WEBHOOK_SECRET_EDITORIAL=

# ── Espace Éditorial — Assistant IA de rédaction (optionnel) ─────────────────
# Clé API Anthropic (console.anthropic.com → API Keys), pour générer des
# brouillons d'édito à relire. Sans elle, le bouton "Générer un brouillon"
# affiche une erreur claire au lieu de planter.
ANTHROPIC_API_KEY=

# ── Espace Éditorial — Génération d'image d'illustration (optionnel) ────────
# Clé API OpenAI (platform.openai.com → API Keys), utilisée pour générer
# l'image d'illustration d'un édito avec DALL·E. Claude ne génère pas
# d'images — c'est pourquoi ce projet utilise deux fournisseurs IA distincts.
# Peut être la même clé qu'un autre projet partageant ce compte OpenAI.
OPENAI_API_KEY=
